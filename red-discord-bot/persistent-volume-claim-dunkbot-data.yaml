apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dunkbot-data
  namespace: red-discord-bot
spec:
  resources:
    requests:
      storage: "1Gi"
  accessModes:
  - ReadWriteOnce
  storageClassName: ceph-block