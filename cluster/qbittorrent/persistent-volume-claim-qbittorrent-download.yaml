apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbittorrent-download
  namespace: media
spec:
  resources:
    requests:
      storage: "901Gi"
  accessModes:
  - ReadWriteMany
  storageClassName: ceph-filesystem