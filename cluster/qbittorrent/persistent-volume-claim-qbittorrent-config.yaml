apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbittorrent-config
  namespace: media
spec:
  resources:
    requests:
      storage: "200Mi"
  accessModes:
  - ReadWriteOnce
  storageClassName: truenas-ssd-iscsi