apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dunkbot-data
  namespace: red-discord-bot
spec:
  resources:
    requests:
      storage: "2Gi"
  accessModes:
  - ReadWriteOnce
  storageClassName: ceph-block