apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nanomq
  namespace: nanomq
spec:
  chart:
    spec:
      chart: app-template
      version: 1.5.x
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  interval: 15m
  timeout: 5m
  releaseName: nanomq
  values:
    image:
      repository: emqx/nanomq
      tag: 0.24.7@sha256:edd1a298fdc36616967d0973442122fd39a2b4b615c92bd435a49b82ee428724

    env:
      NANOMQ_BROKER_URL: "nmq-tcp://0.0.0.0:1883"
      NANOMQ_TLS_ENABLE: "false"
      NANOMQ_WEBSOCKET_ENABLE: "false"
      NANOMQ_HTTP_SERVER_ENABLE: "false"
    envFrom:
      - secretRef:
          name: mqtt-credentials

    service:
      main:
        type: LoadBalancer
        annotations:
          external-dns.alpha.kubernetes.io/hostname: mqtt.${CLUSTER_DOMAIN}
        ports:
          http:
            enabled: false
          mqtt:
            enabled: true
            port: 1883
