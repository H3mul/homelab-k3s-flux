apiVersion: v1
kind: ConfigMap
metadata:
  creationTimestamp: null
  name: speedtest-exporter-helm-chart-value-overrides
  namespace: monitoring
data:
  values.yaml: |-
    image:
      repository: ghcr.io/miguelndecarvalho/speedtest-exporter
      tag: "v3.5.3"

    service:
      main:
        enabled: true
        ports:
          http:
            enabled: false
          metrics:
            enabled: true
            protocol: TCP
            port: 9798
