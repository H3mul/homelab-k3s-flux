apiVersion: v1
kind: Secret
metadata:
  creationTimestamp: null
  name: jackett-config-restic
  namespace: jackett
type: Opaque
stringData:
  RESTIC_REPOSITORY: ${VOLSYNC_MINIO_BUCKET}/jackett-config
  RESTIC_PASSWORD: ${RESTIC_PASSWORD}
  AWS_ACCESS_KEY_ID: ${ACCESS_KEY}
  AWS_SECRET_ACCESS_KEY: ${SECRET_KEY}
