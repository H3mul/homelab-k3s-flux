apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbittorrent-config
  namespace: vpn
spec:
  resources:
    requests:
      storage: "200Mi"
  accessModes:
  - ReadWriteOnce
  storageClassName: ceph-block