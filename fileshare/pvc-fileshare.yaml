apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fileshare
  namespace: media
  labels:
    excluded_from_alerts: "true"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: ceph-filesystem